services:
  nyatet:
    build:
      context: ./serving
    ports:
      - "8000:8000"
    environment:
      - MODEL_DIR=/app/models/tagger
      - CATALOG_PATH=/app/data/catalog.csv
      - NUM_THREADS=1          
