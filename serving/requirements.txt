# Inference only. MUST NEVER gain torch, datasets, or optimum.
# transformers is present for the tokenizer only.
fastapi==0.136.1
uvicorn[standard]==0.46.0
pydantic==2.12.3
onnxruntime==1.19.2
transformers==4.44.2
tokenizers==0.19.1
numpy==2.0.2
